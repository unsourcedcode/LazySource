from setuptools import setup

setup(
   name='LazySource',
   version='1.5',
   description='Tool',
   author='Vaxure',
   author_email='vaxure@protonmail.com',
   install_requires=['gitpython', 'colorama', 'pathlib'], #external packages as dependencies
)