from setuptools import setup

setup(
   name='LazySource',
   version='1.4.6',
   description='Tool',
   author='Vaxure',
   author_email='vaxure@protonmail.com',
   install_requires=['gitpython', 'colorama', 'pathlib', "argparse","urllib"], #external packages as dependencies
)
