from setuptools import setup

setup(
   name='LazySource',
   version='1.5',
   description='Tool',
   author='Vaxure',
   author_email='vaxure@protonmail.com',
   install_requires=['gitpython', 'colorama', 'exchanges', 'pathlib', 'selfupdate'], #external packages as dependencies
)
